# 安全策略

## 支持范围

当前维护的版本为最新稳定 Release。安全相关范围包括：发布包完整性、PowerShell 启动与还原脚本、CDP 注入器，以及本仓库自身处理本地主题文件的逻辑。

主题会让 Codex 以仅回环地址 `127.0.0.1` 开放 Chromium DevTools Protocol。该端口不应视为跨进程安全边界：同一 Windows 用户下的其他本机进程可能访问它。使用主题期间请勿运行不可信的本机程序，并在结束后关闭 Codex 或运行还原脚本。

## 报告漏洞

请勿在公开 Issue 中提交 API Key、会话内容、CDP 地址、日志或可复现敏感数据。请优先使用仓库 Security 页面中的 **Report a vulnerability** 私密报告；若该入口暂不可用，请通过仓库维护者的 GitHub 个人主页联系，并只提供最小必要信息。

报告应包含受影响版本、Windows/Codex/Node.js 版本、复现步骤、影响范围，以及已脱敏的日志片段。维护者会先确认问题，再协调修复与公开披露。
